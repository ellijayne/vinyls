<%= form_for @album do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :artist_id %>
<%= f.select :artist_id, Artist.pluck(:name, :id), :include_blank => true %>

<%= f.label :date %>
<%= f.date_field :date %>

<%# trying to use simple_format below, not sure if syntax is right? %>
<%= f.label :tracks%>
<%= f.text_area :tracks  %>

<%= f.label :image %>
<%= f.file_field :image %>

<!-- <div class="field"> -->
<%# form.label :link %><br>
<!-- this is the choose file button/section -->
<%# form.file_field :link %>
<!-- </div> -->

<%# TODO NEED to add pressing info, possibly in drop down or maybe instead let user input themselves? idea was to use API, dont understand how they work, or the nokogiri thing but also dont understand. %>
<%= f.submit %>
<% end %>
