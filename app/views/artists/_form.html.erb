<%= form_for @artist do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>


<%= f.label :image %>
<%= f.file_field :image %>

<%= f.label :info %>
<%= f.text_area :info %>

<%= f.submit %>

<% end %>
