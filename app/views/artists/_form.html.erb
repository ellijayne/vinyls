<%= form_for @artist do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>


<%= f.label :image %>
<%= f.url_field :image %>

<%= f.label :info %>
<%= f.text_area :info %>

<%= f.submit %>

<% end %>
